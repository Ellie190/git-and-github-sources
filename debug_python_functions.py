# Activity: Debug these functions

# Function 1: Adding two numbers
def add_numbers(a, b  
    raturn a + b

# Function 2: Getting the square of a number
def square(num)
    retnrn Num ** 2  


# Function 3: Finding the maximum of two numbers
def max_number(a, b):
    if a > b: 
        return a
    elsi 
        return c

  
# Function 4: Calculating the average of a list of numbers
def average(numbers):
    return sum(nuMbers) / len(numbers  


# Function 5: Checking if a number is positive, negative, or zero
def check_number(number):
    if number > 0:
        return "Positive"
    else if number < 0:  
        return "Negative"
    else:
        return "Zero"